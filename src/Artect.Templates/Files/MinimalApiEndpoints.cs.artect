// NOTE: This template is intentionally left as a comment-only stub.
// MinimalApiEndpointEmitter builds the output directly via StringBuilder
// (no Handlebars tokens — too many nested conditionals for the .artect DSL).
// The loader entry below is retained so TemplateLoader.Load("MinimalApiEndpoints.cs.artect") succeeds
// and the file remains discoverable in the repo.
//
// Logical name : MinimalApiEndpoints.cs.artect
// Owner emitter: Artect.Generation.Emitters.MinimalApiEndpointEmitter
