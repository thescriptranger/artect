using {{ ErrorsNamespace }};

namespace {{ Namespace }};

public sealed record ApiProblem(
    string Title,
    int Status,
    string? Detail,
    System.Collections.Generic.IReadOnlyList<ValidationError>? Errors);
