// STUB: filled in when the corresponding emitter is implemented in Phase 9.
// Logical name: RepositoryInterface.cs.artect
// Purpose (from plan): I<Entity>Repository in Application; methods ListAsync(page, pageSize, ct), GetByIdAsync(id, ct), CreateAsync(entity, ct), UpdateAsync(entity, ct), DeleteAsync(id, ct). For views and pk-less tables, only ListAsync.
