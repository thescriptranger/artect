// This file is intentionally minimal.
// ProgramCsEmitter builds Program.cs via StringBuilder and loads auth/versioning
// fragments (AuthJwt.cs.artect, AuthAuth0.cs.artect, etc.) via ctx.Templates.Load().
// The main template is not used for rendering; the emitter owns the full structure.
