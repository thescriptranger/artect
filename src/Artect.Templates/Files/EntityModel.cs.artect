namespace {{ Namespace }};

public sealed record {{ EntityName }}Model
{
{{# for col in Columns }}
    public {{ col.ClrTypeWithNullability }} {{ col.PropertyName }} { get; init; }{{ col.Initializer }}
{{/for}}
{{# if HasChildCollections }}

{{# for child in ChildCollections }}
    public System.Collections.Generic.IReadOnlyList<{{ child.TypeName }}Model> {{ child.PropertyName }} { get; init; } = System.Array.Empty<{{ child.TypeName }}Model>();
{{/for}}
{{/if}}
}
