namespace {{ Namespace }};

public sealed record {{ EntityName }}Dto
{
{{# for col in Columns }}
    public {{ col.ClrTypeWithNullability }} {{ col.PropertyName }} { get; init; }{{ col.Initializer }}
{{/for}}
{{# if HasChildCollections }}

{{# for child in ChildCollections }}
    public System.Collections.Generic.IReadOnlyList<{{ child.TypeName }}Dto> {{ child.PropertyName }} { get; init; } = System.Array.Empty<{{ child.TypeName }}Dto>();
{{/for}}
{{/if}}
}
