namespace {{ Namespace }};

public sealed record {{ Kind }}{{ EntityName }}Request
{
{{# for prop in Properties }}
    public {{ prop.ClrTypeWithNullability }} {{ prop.PropertyName }} { get; init; }{{ prop.Initializer }}
{{/for}}
}
