using System.Collections.Generic;

namespace {{ Namespace }};

public abstract record Result<T>
{
    public sealed record Success(T Value) : Result<T>;
    public sealed record Failure(IReadOnlyList<DomainError> Errors) : Result<T>;
}
